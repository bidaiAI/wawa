# mortal_platform extra dependencies
# Install alongside requirements.txt for the platform server

# Encrypted key storage
cryptography>=42.0.0

# Async HTTP (polling AI instances, calling Twitter, fee collection)
aiohttp>=3.9.0

# Twitter OAuth 1.0a
requests>=2.31.0
requests-oauthlib>=1.3.1
